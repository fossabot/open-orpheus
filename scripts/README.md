If you encountered `ERR_MODULE_NOT_FOUND` error, you need to execute the script using `node --loader ./_loader.ts ...`
For Electron, use `pnpm electron -r ./_eloader.ts ...`. If resources are needed, run in root folder.
